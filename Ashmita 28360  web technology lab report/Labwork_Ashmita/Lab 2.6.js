// Define an array of fruits
const fruits = ['Apple', 'Banana', 'Cherry', 'Mango', 'Elderberry'];

// Use the forEach loop to iterate over the array
fruits.forEach(function(fruit, index) {
    console.log(`Fruit ${index + 1}: ${fruit}`);
});
